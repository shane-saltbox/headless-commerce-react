const DEFAULT = null;

const DEBOUNCE_INTERVAL = 2500;

const ISVALID = {
  NO: 'is-invalid',
  UNKNOWN: '',
  YES: 'is-valid',
};

const SOURCE = '';

const WS_STATUS = {
  DURATION: 1000,
  STATE: {
    ACTIVE: 'active',
    COMPLETE: 'complete',
    IDLE: 'idle',
  },
};

export { DEFAULT as default };
export { DEBOUNCE_INTERVAL };
export { ISVALID };
export { SOURCE };
export { WS_STATUS };
